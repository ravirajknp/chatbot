const toggle = () => {
  const bot = document.querySelector("#bot");
  const botButton  = document.querySelector("#botButton");
  bot?.classList?.toggle("hide");
  botButton?.classList?.toggle("hide");
}
